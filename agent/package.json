{
  "name": "sentinellium-agent",
  "version": "1.0.0",
  "description": "Frida agent for Sentinellium RASP auditing framework",
  "private": true,
  "scripts": {
    "build": "frida-compile src/core/index.ts -o _agent.js",
    "watch": "frida-compile src/core/index.ts -o _agent.js -w",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/frida-gum": "^18.7.1",
    "frida-compile": "^16.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
